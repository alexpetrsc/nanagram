#pragma once

#include <map>
#include <vector>
#include <string>

class ConsoleView{

public:

    void print(

        const std::map<

            std::string,

            std::vector<std::string>

        >&

    );

};
